name: Generate APK
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4 # v3 से v4 कर दिया

      - name: set up JDK 17
        uses: actions/setup-java@v4 # v3 से v4 कर दिया
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      - name: Setup Gradle
        run: gradle wrapper

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build Debug APK
        run: ./gradlew assembleDebug

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4 # v3 से v4 कर दिया (यही मेन एरर था)
        with:
          name: SabkaFayda-APK
          path: app/build/outputs/apk/debug/app-debug.apk
